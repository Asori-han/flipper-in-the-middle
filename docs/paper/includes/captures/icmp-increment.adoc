= Captura ICMP con payload incremental

ifndef::capture-images-dir[:capture-images-dir: ../../../../build/captures]

== Evidencia utilizada

.Archivos y artefactos de la captura ICMP incremental
[cols="2,4"]
|===
|Guion |`scope-increment.eth`
|NPZ saneado |`data/captures/ethernet/20260913_031230_increment.npz`
|Artefactos |`build/captures/icmp-increment.*`
|===

== Señal analógica y analizador lógico

El payload ICMP contiene la secuencia `00` a `1f`.

.Señal Ethernet diferencial de la prueba incremental
image::{capture-images-dir}/icmp-increment_full.png[Señal analógica ICMP incremental,align="center"]

.Detalle de la señal analógica y decodificación Manchester
image::{capture-images-dir}/icmp-increment_overlay.png[Overlay analógico ICMP incremental,align="center"]

.Señal digital SPI y campos presentes en el cable
image::{capture-images-dir}/icmp-increment_spi.png[Comparación digital SPI y trama física ICMP incremental,align="center"]

== Salida HEX

.Trama ICMP incremental reconstruida en hexadecimal
[source,text]
----
include::../../../../build/captures/icmp-increment.hex[]
----
