# Python dependencies

- `lab.txt`: analysis, export, plots, animations and tests.
- `demo.txt`: synchronized hardware capture with Logic 2, DOS1102 and Flipper Zero.

Create a virtual environment and install the required set:

```sh
python3 -m venv build/cache/venv
build/cache/venv/bin/pip install -r requirements/lab.txt
```
